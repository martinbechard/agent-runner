# Rendered HTML design-system suite template

Copy the complete directory through the repository file-placement mechanism. Replace every bracketed placeholder and every specimen with source-grounded target-state content. Keep shared suite navigation, previous/next navigation, live visual examples, source references, responsive behavior, and `VERSION` synchronized across pages.

Required pages: `index.html`, `foundations.html`, `application-shell.html`, `content.html`, `data-display.html`, `forms-and-actions.html`, `visualizations.html`, `states-and-feedback.html`, `accessibility.html`, `variations.html`, and `source-inventory.html`.

The suite defines the intended end state only. Put baseline gaps, migration sequencing, statuses, and implementation evidence in the separate conformance template. Catalog CSS and JavaScript demonstrate the system; they are not runtime product source.

components:
  - id: "CMP-001-cli-app"
    name: "Python command-line application"
    responsibility: "Owns the runnable hello-world command-line workflow, the run-instructions artifact, and the single automated output-verification path for the project."
    technology: "Python 3 with pytest"
    feature_realization_map:
      FT-001: "Implements the command-line entry point that prints `Hello, world!` to standard output when a human runs the application."
      FT-002: "Owns the README-based run instructions that tell a human how to invoke the application from the command line."
      FT-003: "Owns the single automated test and test-suite invocation path that verify the program prints the expected output."
    dependencies: []
interactions: []
